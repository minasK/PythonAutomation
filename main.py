import re, time, os

import tkinter as tk
from tkinter import filedialog as fd
from tkinter import messagebox
# def my_function_with_args(username, greeting):
#     print("Hello %s From My Function!, I wish you %s"%(username, greeting))
#
#
# minas  = input("username input\n")
# lam = input("greeting input\n")
# my_function_with_args(minas, lam)
#
# utility= input("what utility do you want to search for")
# os.system("%s/?" %utility)
#
# print(os.name)

x=input("write")  #while true and stopping with break
while True:
    print("hi")
    if x == "1":
        pass
    else:
        break


        # Get the file path
        # def browse(self):
        #     filetypes = (
        #         ('text files', '*.txt'),
        #         ('All files', '*.*')
        #     )
        #
        #     cd = "/" + os.getcwd()
        #
        #     path = fd.askopenfilename(
        #         title='Select File',
        #         initialdir=cd,
        #         filetypes=filetypes
        #     )
        #
        #     self.path.set(path)


        # File path: Label, Entry, and Button
        # self.file_label = tk.Label(self, text="Select a text file:")
        # self.file_label.grid(row=0, column=0)
        #
        # self.path = tk.StringVar()
        # self.path_entry = tk.Entry(self, textvariable=self.path)
        # self.path_entry.grid(row=0, column=1)
        #
        # self.browse_button = tk.Button(self, text='Select')
        # self.browse_button['command'] = self.browse
        # self.browse_button.grid(row=0, column=2, padx=20, pady=20)


